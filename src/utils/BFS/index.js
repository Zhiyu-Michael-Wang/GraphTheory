const shortestPath = require('./shortestPath')
const shortestPathGrid = require('./shortestPathGrid')


const BFS = {
    shortestPath: shortestPath,
    shortestPathGrid: shortestPathGrid
}


module.exports = BFS